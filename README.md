SDEV255_FINAL_PROJECT_GROUP3

Frontend is hosted on GitHub Pages, backend API is deployed on Render, and the MySQL database is hosted on Railway. The application supports full CRUD operations and communicates across services using RESTful API endpoints.